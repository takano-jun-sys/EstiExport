# GAS実装手順ガイド

このガイドでは、EstiExportをGoogle Apps Scriptで実装する詳細な手順を説明します。

## 前提条件
- Googleアカウント
- Googleドライブへのアクセス
- 基本的なスプレッドシート操作の知識

## 実装手順

### ステップ1: Googleスプレッドシートを作成

1. **Googleドライブを開く**
   - https://drive.google.com/ にアクセス

2. **新しいスプレッドシートを作成**
   - 左上の「新規」ボタンをクリック
   - 「Google スプレッドシート」→「空白のスプレッドシート」を選択

3. **スプレッドシートに名前を付ける**
   - 左上の「無題のスプレッドシート」をクリック
   - 「見積書管理システム」などの名前を入力

### ステップ2: Apps Scriptエディタを開く

1. **拡張機能メニューを開く**
   - スプレッドシート上部のメニューから「拡張機能」をクリック

2. **Apps Scriptを開く**
   - 「Apps Script」を選択
   - 新しいタブでApps Scriptエディタが開きます

### ステップ3: スクリプトファイルを作成

#### 3-1. デフォルトのCode.gsを置き換え

1. **既存のCode.gsを開く**
   - 左側のファイル一覧で「Code.gs」が選択されている状態

2. **既存のコードを削除**
   - エディタ内の`function myFunction() {}`を全て削除

3. **Code.gsの内容をコピー**
   - このリポジトリの`Code.gs`の内容を全てコピー
   - エディタに貼り付け

4. **保存**
   - Ctrl+S（Mac: Cmd+S）で保存
   - または上部の保存アイコンをクリック

#### 3-2. Config.gsを追加

1. **新しいスクリプトファイルを作成**
   - エディタ左側の「ファイル」の横にある「+」アイコンをクリック
   - 「スクリプト」を選択

2. **ファイル名を変更**
   - 作成された「無題」ファイルの右にある3点メニューをクリック
   - 「名前を変更」を選択
   - 「Config」と入力（.gsは自動で付きます）

3. **Config.gsの内容をコピー**
   - このリポジトリの`Config.gs`の内容を全てコピー
   - エディタに貼り付け

4. **保存**
   - Ctrl+S（Mac: Cmd+S）で保存

#### 3-3. QuoteGenerator.gsを追加

上記と同じ手順で以下のファイルを追加します：

1. **ファイルを作成**
   - 「+」→「スクリプト」

2. **名前を変更**
   - 「QuoteGenerator」

3. **内容をコピー**
   - このリポジトリの`QuoteGenerator.gs`の内容を貼り付け

4. **保存**

#### 3-4. PDFExporter.gsを追加

1. **ファイルを作成**
   - 「+」→「スクリプト」

2. **名前を変更**
   - 「PDFExporter」

3. **内容をコピー**
   - このリポジトリの`PDFExporter.gs`の内容を貼り付け

4. **保存**

#### 3-5. Setup.gsを追加

1. **ファイルを作成**
   - 「+」→「スクリプト」

2. **名前を変更**
   - 「Setup」

3. **内容をコピー**
   - このリポジトリの`Setup.gs`の内容を貼り付け

4. **保存**

### ステップ4: appsscript.jsonを設定

1. **プロジェクト設定を開く**
   - エディタ左側の歯車アイコン（プロジェクトの設定）をクリック

2. **「appsscript.json」マニフェスト ファイルをエディタで表示する**
   - このチェックボックスをONにする

3. **appsscript.jsonを開く**
   - 左側のファイル一覧に「appsscript.json」が表示されるのでクリック

4. **既存の内容を置き換え**
   - このリポジトリの`appsscript.json`の内容をコピー
   - エディタに貼り付け（既存の内容を上書き）

5. **保存**
   - Ctrl+S（Mac: Cmd+S）で保存

### ステップ5: 初期セットアップを実行

1. **Setup.gsを開く**
   - 左側のファイル一覧で「Setup.gs」をクリック

2. **setupSheets関数を選択**
   - エディタ上部の「実行する関数を選択」ドロップダウンをクリック
   - 「setupSheets」を選択

3. **実行**
   - 「実行」ボタン（▶アイコン）をクリック

4. **権限を承認**
   - 初回実行時に「承認が必要です」ダイアログが表示されます
   - 「権限を確認」をクリック
   - Googleアカウントを選択
   - 「詳細」→「見積書管理システム（安全ではないページ）に移動」をクリック
   - 「許可」をクリック

5. **実行完了を確認**
   - 「実行が完了しました」メッセージが表示されます
   - スプレッドシートに戻ると、3つのシートが作成されています
     - 見積データ
     - 見積書テンプレート
     - 設定

### ステップ6: 会社情報を設定

1. **スプレッドシートに戻る**
   - ブラウザのタブで元のスプレッドシートを開く

2. **「設定」シートを開く**
   - 下部のシートタブから「設定」をクリック

3. **会社情報を編集**
   - B列の値を自社の情報に書き換えます：
     - 会社名
     - 郵便番号
     - 住所
     - 電話番号
     - FAX
     - メールアドレス
     - 銀行名
     - 支店名
     - 口座種別
     - 口座番号
     - 口座名義

### ステップ7: テンプレートをカスタマイズ（任意）

1. **「見積書テンプレート」シートを開く**

2. **デザインを調整**
   - フォント、色、レイアウトなどを自由に変更できます
   - セルの位置は`Config.gs`の`TEMPLATE_CELLS`で定義されているので注意

3. **保護を解除する場合**
   - 「データ」→「シートと範囲を保護」
   - 保護設定を削除してから編集

### ステップ8: 動作確認

#### 手動で見積書を作成してテスト

1. **「見積データ」シートを開く**

2. **サンプルデータを確認**
   - 既にサンプルデータが1行入っています（2行目）

3. **ステータスを「未作成」に設定**
   - L2セル（ステータス）が「未作成」になっていることを確認

4. **スクリプトを実行**
   - 「拡張機能」→「Apps Script」でエディタに戻る
   - 「実行する関数を選択」で「processNewQuotes」を選択
   - 「実行」をクリック

5. **結果を確認**
   - スプレッドシートに戻る
   - M列とN列にURLが入っていることを確認
   - L列のステータスが「作成済み」になっていることを確認
   - M列のURLをクリックして見積書スプレッドシートを確認
   - N列のURLをクリックしてPDFを確認

### ステップ9: 自動処理の設定（任意）

定期的に自動で見積書を作成する場合：

1. **Apps Scriptエディタを開く**

2. **トリガーを設定**
   - 左側のメニューから「トリガー」（時計アイコン）をクリック
   - 右下の「トリガーを追加」をクリック

3. **トリガー設定**
   - 実行する関数を選択: `processNewQuotes`
   - 実行するデプロイを選択: `Head`
   - イベントのソースを選択: `時間主導型`
   - 時間ベースのトリガーのタイプを選択: `分ベースのタイマー`
   - 時間の間隔を選択: `5分おき`
   - 「保存」をクリック

4. **確認**
   - トリガー一覧に追加されたことを確認

または、`Setup.gs`の`setupTrigger()`関数を実行しても同じことができます。

### ステップ10: AppSheetとの連携

1. **AppSheetにログイン**
   - https://www.appsheet.com/

2. **新しいアプリを作成**
   - 「Create」→「App」→「Start with existing data」

3. **データソースを選択**
   - 「Google Sheets」を選択
   - 作成したスプレッドシートを選択
   - 「見積データ」シートを選択

4. **AppSheetで入力フォームを作成**
   - 顧客情報や明細を入力できるフォームを作成
   - 明細JSONは、AppSheetの仮想列やActionで生成可能

5. **テスト**
   - AppSheetからデータを入力
   - ステータスを「未作成」に設定
   - 数分待つ（トリガーが実行される）
   - スプレッドシートとPDFが自動生成されることを確認

## トラブルシューティング

### エラー: 「見積データシートが見つかりません」

**原因**: シートの初期セットアップが完了していない

**解決方法**:
1. `Setup.gs`の`setupSheets()`関数を実行
2. スプレッドシートを更新して3つのシートが作成されたか確認

### エラー: 「ReferenceError: CONFIG is not defined」

**原因**: Config.gsが正しく読み込まれていない

**解決方法**:
1. Config.gsファイルが存在することを確認
2. Config.gsの内容が正しく貼り付けられているか確認
3. すべてのファイルを保存
4. ページを再読み込み

### PDFが生成されない

**原因**: 権限不足またはドライブ容量不足

**解決方法**:
1. Google Driveの容量を確認
2. スクリプトの権限を再承認
   - Apps Scriptエディタで関数を再実行
   - 権限を再度承認

### 明細が正しく表示されない

**原因**: 明細JSONのフォーマットが間違っている

**解決方法**:
1. 「見積データ」シートのH列（明細JSON）の形式を確認
2. 正しいJSON形式で入力されているか確認
3. サンプルデータを参考にする

```json
[
  {
    "item": "商品名",
    "quantity": 1,
    "unit": "個",
    "unitPrice": 10000,
    "amount": 10000,
    "note": "備考"
  }
]
```

## Tips

### カスタムメニューの使用

スプレッドシートを開くと、自動的に「見積書システム」メニューが追加されます：

- **新規見積書を処理**: 手動で未作成の見積書を処理
- **選択行の見積書を再生成**: 特定の行の見積書を再作成
- **初期セットアップ**: シートを再セットアップ

### 明細の追加方法（AppSheet）

AppSheetで明細を入力しやすくするために：

1. 別シート「明細」を作成
2. 見積番号で「見積データ」とリレーション
3. AppSheetで明細入力フォームを作成
4. Actionで明細をJSON化して「見積データ」のH列に書き込む

### デバッグ方法

エラーが発生した場合：

1. **実行ログを確認**
   - Apps Scriptエディタ
   - 下部の「実行ログ」をクリック
   - エラーメッセージを確認

2. **Logger.log()を追加**
   - スクリプト内に`Logger.log('デバッグ情報');`を追加
   - 実行後、「実行ログ」で確認

## まとめ

これでAppSheet連携の見積書作成システムが完成しました！

**次のステップ**:
1. 実際のビジネスデータで動作確認
2. テンプレートを会社のデザインに合わせてカスタマイズ
3. AppSheetアプリを作成して運用開始

質問や問題がある場合は、実行ログを確認してエラーメッセージを特定してください。
